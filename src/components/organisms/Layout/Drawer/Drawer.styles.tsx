import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    paddingTop: 0,
  },
  headerContainer: {
    flex: 1,
    flexDirection: 'row',
    padding: 16,
  },
  headerText: {
    fontSize: 16,
    fontWeight: '400',
  },
});

export default styles;
